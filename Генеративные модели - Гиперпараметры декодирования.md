---
type: note
status: done
tags: [ml/nlp]
sources:
-
authors:
-
---



### Гиперпараметры декодирования в генеративных моделях

После того как модель-декодер (например, GPT) обработала входную последовательность, на самом последнем слое она выдает **логиты** — сырые, ненормализованные оценки для каждого токена в словаре. Чтобы превратить их в вероятности, применяется функция **Softmax**.

Эти гиперпараметры как раз и управляют тем, как мы **выбираем (сэмплируем)** следующий токен из этого распределения вероятностей.


### 1. Greedy Decoding (Жадное декодирование)

Это самый простой и интуитивно понятный метод.

- **Принцип работы:** На каждом шаге мы всегда выбираем токен с самой высокой вероятностью.
- **Проблема:** Текст получается очень предсказуемым, скучным и часто зацикливается. Модель быстро попадает в ловушку повторения одних и тех же фраз, так как самые вероятные слова часто являются наиболее общими.
- **Применение:** Практически никогда не используется для генерации текста.

---

### 2. Temperature (Температура)

Этот параметр контролирует "уверенность" модели и уровень случайности.

- **Принцип работы:** Мы делим логиты на значение температуры **перед** применением Softmax.
  $$ \text{new\_logits} = \frac{\text{logits}}{\text{temperature}} $$
- **`temperature` < 1.0 (например, 0.7):** Делает распределение более "острым". Вероятности более вероятных токенов увеличиваются, а менее вероятных — уменьшаются. Модель становится более уверенной и сфокусированной.
	- **Эффект:** Текст более предсказуемый, последовательный и связный.
	- **Применение:** Генерация кода, ответы на фактические вопросы, следование инструкциям.
- **`temperature` > 1.0 (например, 1.5):** Делает распределение более "плоским". Разница между вероятностями токенов сглаживается, что увеличивает шанс выбора менее очевидных слов.
	- **Эффект:** Текст более случайный, "творческий", иногда — бредовый.
	- **Применение:** Креативное письмо, мозговой штурм, генерация разнообразных идей.
- **`temperature` = 1.0:** Никак не изменяет исходные вероятности.
- **`temperature` -> 0:** Приближается к жадному декодированию (Greedy Decoding).

---

### 3. Top-K Sampling

Этот метод ограничивает выборку фиксированным числом наиболее вероятных токенов.

- **Принцип работы:**
	1. Сортируем все токены по их вероятности.
	2. Выбираем только `K` самых вероятных токенов (например, K=50).
	3. Перераспределяем (нормализуем) вероятности между этими `K` токенами так, чтобы их сумма снова была равна 1.
	4. Сэмплируем следующий токен из этого усеченного набора.
- **Эффект:** Мы отсекаем "хвост" распределения, состоящий из тысяч маловероятных и часто неуместных токенов. Это делает текст более связным, не давая модели случайно выбрать какой-нибудь абсурдный токен.
- **Проблема:** Метод неадаптивен. Иногда правильный следующий токен может быть очевиден (и `K=50` — это слишком много), а иногда распределение очень плоское, и правильное слово может не попасть в топ-50.

---

### 4. Top-P (Nucleus) Sampling

Это более умный и адаптивный метод, который сейчас используется чаще всего.

- **Принцип работы:** Вместо выбора фиксированного числа `K` токенов, мы выбираем наименьший набор токенов, чья **суммарная вероятность** превышает порог `P` (например, P=0.95).
	1. Сортируем токены по вероятности от большего к меньшему.
	2. Начинаем суммировать их вероятности, пока сумма не достигнет `P`.
	3. Все токены, которые вошли в эту сумму, формируют наше "ядро" (nucleus) для сэмплирования.
	4. Вероятности внутри этого ядра нормализуются, и из них выбирается следующий токен.
- **Эффект:** Размер набора для сэмплирования становится **динамическим**.
	- Если модель очень **уверена** в следующем слове (например, после "to be or not to" слово "be" имеет вероятность 98%), то ядро будет состоять всего из одного этого слова (`P=0.95`).
	- Если модель **не уверена** и вероятности распределены равномерно между многими словами, ядро будет большим, давая модели больше пространства для выбора.
- **Применение:** Считается лучшим компромиссом между связностью и креативностью. `P=0.9-0.95` — стандартное значение для большинства задач.

---

### 🃏/key-questions
**Key Questions:**

1. Что такое логиты и как они превращаются в вероятности?
?
- **Логиты** — это сырые, ненормализованные оценки для каждого токена в словаре, которые выдает последний слой модели.
- Они превращаются в вероятности с помощью функции **Softmax**, которая "сжимает" все значения в диапазон [0, 1] так, чтобы их сумма была равна 1.

2. Как работает `temperature` и как ее значения влияют на генерацию?
?
- **Как работает:** Делит вектор логитов на свое значение перед Softmax.
- **Влияние:**
	- `T < 1`: Повышает уверенность модели, делает текст более сфокусированным.
	- `T > 1`: Снижает уверенность, делает текст более случайным и "творческим".

3. В чем ключевое различие между Top-K и Top-P (Nucleus) сэмплированием?
?
- **Top-K** всегда выбирает фиксированное число (`K`) самых вероятных токенов.
- **Top-P** выбирает динамическое число токенов, чья суммарная вероятность превышает порог `P`. Это делает его более адаптивным к "уверенности" модели на каждом шаге.
